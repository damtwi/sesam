# Fortran settings
FC = mpiifort
LD = mpiifort
CPPFLAGS = -DMPI
CPPDEFS = -DMPI

FFLAGS = -fpic -132 -convert big_endian $(IDEV_PATH) -I${HOME}/include_mpi \
         -I/linkhome/rech/genige01/utw56cf/ensdam-master/build/python/src \
         -I/gpfslocalsup/pub/miniforge/24.11.3/pkgs/netcdf-fortran-4.6.2
#FFLAGS = -fpic -132 -convert big_endian $(IDEV_PATH) -I${HOME}/include_mpi \
         -I/linkhome/rech/genige01/utw56cf/ensdam-master/build/python/src
#LDFLAGS = -lnetcdff -L$(HOME)/lib -lensdam -lflowsampler
#LDFLAGS = -lnetcdff -L$(HOME)/lib -lensdam_mpi
LDFLAGS = -L/gpfslocalsup/pub/miniforge/24.11.3/pkgs/netcdf-fortran-4.6.2-nompi_hee1cade_100/lib \
          -lnetcdff -lnetcdf -L$(HOME)/lib -lensdam_mpi
LIST =

# f2py settings
NCDFLIB = /smplocal/pub/NetCDF/4.1.3/seq/lib/
FVENDOR = intelem
FCOMPILER = /smplocal/pub/Modules/IDRIS/wrappers/mpiifort
F2PYFLAGS = --fcompiler=$(FVENDOR) --f90exec=$(FCOMPILER)

